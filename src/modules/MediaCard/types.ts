export interface MediaCardProps {
  isOnThumbnail?: boolean;
  isBookmarked?: boolean;
  textYear: number;
  textCategory: string;
  textRating: string;
  textTitle: string;
  thumbnailSrc: string;
  id: string;
}
