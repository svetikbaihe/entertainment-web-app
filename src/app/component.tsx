import { CssBaseline } from "@mui/material";
import React, { StrictMode } from "react";

const App: React.FC = () => {
	return (
		<StrictMode>
			<CssBaseline></CssBaseline>
		</StrictMode>
	);
};

export default App;
